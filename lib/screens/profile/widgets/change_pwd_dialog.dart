import 'package:flutter/material.dart';

class ChangePwdDialogWidget extends StatelessWidget {
  const ChangePwdDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
